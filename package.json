{
  "name": "demos",
  "version": "1.0.0",
  "description": "-   when you don't see an element, check it's position, display, z-index, height and width",
  "main": "fetchSlots.js",
  "scripts": {
    "build": "tsc",
    "build2": "tsc && copyfiles -f src/*.js src/*.sh src/doctorDetails.json dist/"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "yahoo-finance2": "^2.13.3"
  },
  "devDependencies": {
    "@types/node": "^22.10.3",
    "copyfiles": "^2.4.1",
    "typescript": "^4.0.0"
  }
}
